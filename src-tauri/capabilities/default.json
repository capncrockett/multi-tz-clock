{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main clock window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
